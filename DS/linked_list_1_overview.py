class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

# Reverse the linked list that starts with "head" listNode
def reverse(head):
    prev = None
    while head:
        # Save next
        # Reverse node
        # Update prev
        # Move forward



# Merge two linked lists according to a ListNode condition
def merge(l1, l2):
    def l1_select(node1, node2):
        # Element selection condition

    # Init dummy node

    # while both lists not empty
        # Add elem to merged according to condition
    # If one list not empty:
        # cur.next = non empty list pointer

    return dummy.next
    